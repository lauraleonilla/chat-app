jobs:
  deploy:
    executor: heroku/default
    steps:
      - checkout
      - heroku/install
      - run: release-docker-image
orbs:
  heroku: circleci/heroku@0.0.10
version: 2.1
workflows:
  heroku_deploy:
    jobs:
      - deploy